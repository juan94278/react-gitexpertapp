import GitExpertApp from '../src/GifExpertApp';
import '@testing-library/react';

describe( 'Debe de capturar el contenido principal del proyecto', () => {
    test('Probando arranque', () => {
        const wapper = <GitExpertApp></GitExpertApp>

        
    })
})